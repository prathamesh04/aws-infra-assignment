{
  "name": "sample-app",
  "version": "1.0.0",
  "description": "Sample Node.js application for the infrastructure assignment",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage test/*.test.js"
  },
  "dependencies": {
    "dd-trace": "^5.0.0",
    "express": "^4.19.2",
    "winston": "^3.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
